// Chat session hooks
// Implementation in task 20.3
export function useChat() {
  // Placeholder — will manage chat session state and message sending
  return { messages: [], sendMessage: async (_content: string) => {} };
}
